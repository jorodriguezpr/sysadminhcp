--- vqadmin-2.3.7/configure.in.old	2005-01-11 13:32:40.000000000 -0700
+++ vqadmin-2.3.7/configure.in	2025-09-10 17:37:49.751725764 -0600
@@ -1,7 +1,12 @@
 dnl Process this file with autoconf to produce a configure script.
-AC_INIT(vqadmin.c)
-AM_INIT_AUTOMAKE(vqadmin,2.3.6)
-AM_CONFIG_HEADER(config.h)
+AC_INIT(vqadmin.c,2.3.7)
+AC_CONFIG_SRCDIR(vqadmin.c)
+AM_INIT_AUTOMAKE
+AC_CONFIG_HEADERS([config.h])
+
+dnl Set up system extensions first.
+AC_USE_SYSTEM_EXTENSIONS
+
 AC_CANONICAL_HOST
 
 dnl Checks for programs.
@@ -9,13 +14,18 @@
 AC_PROG_RANLIB
 
 AC_ISC_POSIX
-AC_AIX
 
 if test "$GCC" = yes ; then
         CXXFLAGS="$CXXFLAGS -Wall"
         CFLAGS="$CFLAGS -Wall"
 fi
 
+AC_DEFINE([ENABLE_ISOQLOG], [], [Description])
+AC_DEFINE([ISOQLOGPATH], [], [Description])
+AC_DEFINE([QMAILDIR], [], [Description])
+AC_DEFINE([VPOPGROUP], [], [Description])
+AC_DEFINE([VPOPUSER], [], [Description])
+
 qmaildir=""
 for f in /var/qmail
 do
@@ -200,7 +210,6 @@
 
 dnl Checks for header files.
 AC_HEADER_DIRENT
-AC_HEADER_STDC
 AC_CHECK_HEADERS(unistd.h)
 
 dnl Checks for typedefs, structures, and compiler characteristics.
