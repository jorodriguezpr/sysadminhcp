--- simscan-1.4.0.final/simscan.c	2021-04-17 15:11:05.236275613 -0600
+++ simscan-1.4.0.final/simscan.new.c	2021-05-21 09:49:05.979945000 -0600
@@ -302,15 +302,15 @@
   format_dir(workdir);
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: starting: work dir: %s\n", workdir);  
+    fprintf(stderr, "simscan:[%d]: starting: work dir: %s\n",getppid(), workdir);  
   }
 
   /* create the working directory, allow group access too */
   umask ( 027 ) ;
   if ( mkdir(workdir, 0750) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error making work dir, exit 400, errno: %d\n",
-        errno);
+      fprintf(stderr, "simscan:[%d]: error making work dir, exit 400, errno: %d\n",
+        getppid(),errno);
     }
     _exit(EXIT_400);
   }
@@ -319,8 +319,8 @@
   if ( chdir(workdir) != 0 ) {
     if ( DebugFlag > 0 ) {
       fprintf(stderr, 
-        "simscan: error changing directory to workdir errno: %d\n",
-        errno);
+        "simscan:[%d]: error changing directory to workdir errno: %d\n",
+        getppid(),errno);
     }
     exit_clean(EXIT_400);
   }
@@ -329,8 +329,8 @@
   snprintf(message_name, sizeof(message_name), "msg.%s", unique_ext);
   if ( (fd=open(message_name, O_WRONLY|O_CREAT|O_TRUNC,0644)) ==- 1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error opening msg file %s errnostr: %d\n", 
-        message_name, errno);
+      fprintf(stderr, "simscan:[%d]: error opening msg file %s errnostr: %d\n", 
+        getppid(),message_name, errno);
     }
     exit_clean(EXIT_400);
   }
@@ -339,7 +339,7 @@
   while( (ret = read(0, buffer, sizeof(buffer))) > 0 ) {
     if ( write(fd, buffer,ret) == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: error writing msg error: %d\n", errno);
+        fprintf(stderr, "simscan:[%d]: error writing msg error: %d\n", getppid(),errno);
       }
       /* on a write error close the file so we can remove the directory */
       close(fd);
@@ -350,7 +350,7 @@
   /* close the file */
   if ( close(fd) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error closing email file errno: %d\n", errno);
+      fprintf(stderr, "simscan:[%d]: error closing email file errno: %d\n",getppid(), errno);
     }
     exit_clean(EXIT_400);
   }
@@ -364,7 +364,7 @@
   /* open the addr_name file */
   if ( (fd_per=open(addr_name, O_WRONLY|O_CREAT|O_TRUNC,0644)) ==- 1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error opening addr name: %s\n", addr_name);
+      fprintf(stderr, "simscan:[%d]: error opening addr name: %s\n", getppid(),addr_name);
     }
     exit_clean(EXIT_400);
   }
@@ -406,7 +406,7 @@
         strncpy(MailFrom, &addr_buff[1], sizeof(MailFrom)-1);
         gotfrom = 1;
         if ( DebugFlag > 3 )
-          fprintf(stderr, "simscan: F envelope is %s\n", MailFrom);
+          fprintf(stderr, "simscan:[%d]: F envelope is %s\n", getppid(),MailFrom);
      }
      if (addr_buff[0] == 'T') {
         if (MaxRcptTo<MAX_RCPT_TO) {
@@ -414,7 +414,7 @@
            gotrcpt = 1;
            MaxRcptTo ++;
            if ( DebugFlag > 3 )
-              fprintf(stderr, "simscan: T%d envelope is %s\n", MaxRcptTo, RcptTo[MaxRcptTo-1]);
+              fprintf(stderr, "simscan:[%d]: T%d envelope is %s\n", getppid(),MaxRcptTo, RcptTo[MaxRcptTo-1]);
         }
      }
    }
@@ -430,7 +430,7 @@
 
   if ( MailFrom[0] == 0 && RcptTo[0][0] == 0 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: no envelope information, deferred exit\n");
+      fprintf(stderr, "simscan:[%d]: no envelope information, deferred exit\n",getppid());
     }
     exit_clean(EXIT_454);
   }
@@ -490,7 +490,7 @@
   /* break the email msg into mime parts */
   if ( run_ripmime() != 0 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: ripmime error\n");
+      fprintf(stderr, "simscan:[%d]: ripmime error\n",getppid());
     }
     exit_clean(EXIT_400);
   }
@@ -521,29 +521,29 @@
   ret = check_clam();
   switch ( ret ) {
     case -2: 
-      if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: clamdscan disabled\n"); }
+      if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: clamdscan disabled\n",getppid()); }
       break;
     case -1: 
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: fatal error executing clamdscan\n");
+        fprintf(stderr, "simscan:[%d]: fatal error executing clamdscan\n",getppid());
       }
       exit_clean(EXIT_400);
       break;
     case 1:
       FoundVirus=1;
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: clamdscan detected a virus\n");
+        fprintf(stderr, "simscan:[%d]: clamdscan detected a virus\n",getppid());
       }
       break;
     case 2:
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: fatal error executing clamdscan\n");
+        fprintf(stderr, "simscan:[%d]: fatal error executing clamdscan\n",getppid());
       }
       exit_clean(EXIT_400);
       break;
     default: 
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: normal clamdscan return code: %d\n", ret);
+        fprintf(stderr, "simscan:[%d]: normal clamdscan return code: %d\n",getppid(), ret);
       }
       break;
   }
@@ -555,22 +555,22 @@
   switch(ret) {
     case 0:
       if ( DebugFlag > 0 ) {
-       fprintf(stderr, "simscan: trophie found no virus\n");
+       fprintf(stderr, "simscan:[%d]: trophie found no virus\n",getppid());
       }
       break;
     case 1:
       if ( DebugFlag > 0 ) {
-       fprintf(stderr, "simscan: trophie found virus\n");
+       fprintf(stderr, "simscan:[%d]: trophie found virus\n",getppid());
       }
       FoundVirus=1;
       break;
     case -1:
       /* disabled */
-      if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: trophie disabled\n"); }
+      if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: trophie disabled\n",getppid()); }
       break;
     default:
       if ( DebugFlag > 0 ) {
-       fprintf(stderr, "simscan: some temp. error occured with trophie\n");
+       fprintf(stderr, "simscan:[%d]: some temp. error occured with trophie\n",getppid());
       }
       exit_clean(EXIT_400);
   }
@@ -609,7 +609,7 @@
     /* re-open the file read only */
     if ( (fd = open(message_name, O_RDONLY)) == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: spam can not open file: %s\n", message_name);
+        fprintf(stderr, "simscan:[%d]: spam can not open file: %s\n",getppid(), message_name);
       }
       exit_clean(EXIT_400);
     }
@@ -617,7 +617,7 @@
     /* set the standard input to be the new file */
     if ( fd_move(0,fd)  == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: WARNING: spam could not fd_move\n");
+        fprintf(stderr, "simscan:[%d]: WARNING: spam could not fd_move\n",getppid());
       }
       exit_clean(EXIT_400);
     }
@@ -625,7 +625,7 @@
     /* optionally check for spam with spamassassin */ 
     snprintf(spam_message_name, sizeof(spam_message_name), "spam.msg.%s", unique_ext);
     if ( DebugFlag > 0 ) {
-    	  fprintf(stderr, "simscan: check_spam will now read %s and write to %s\n", message_name, spam_message_name);
+    	  fprintf(stderr, "simscan:[%d]: check_spam will now read %s and write to %s\n",getppid(), message_name, spam_message_name);
     }
     ret = check_spam();
     switch ( ret ) {
@@ -638,7 +638,7 @@
       /* spam detected, refuse message */
       case 1:
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: check_spam detected spam refuse message\n");
+          fprintf(stderr, "simscan:[%d]: check_spam detected spam refuse message\n",getppid());
         }
         close(fd);
 
@@ -649,7 +649,7 @@
 	      
 #ifdef ENABLE_DROPMSG
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: droping the message\n");
+          fprintf(stderr, "simscan:[%d]: droping the message\n",getppid());
         }
         exit_clean(EXIT_0);
         /* Drop the message, returning success to sender. */
@@ -673,7 +673,7 @@
         /* errors , return temporary error */
       default:
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: check_spam had an error ret: %d\n", ret);
+          fprintf(stderr, "simscan:[%d]: check_spam had an error ret: %d\n",getppid(), ret);
         }
         close(fd);
         exit_clean(EXIT_400); 
@@ -681,7 +681,7 @@
 #ifdef ENABLE_PER_DOMAIN
   }
   else {
-    fprintf(stderr, "simscan: skipping check_spam, disabled for this domain\n");
+    fprintf(stderr, "simscan:[%d]: skipping check_spam, disabled for this domain\n",getppid());
   }
 #endif /* ENABLE_PER_DOMAIN */
 #endif /* ENABLE_SPAM */
@@ -692,11 +692,11 @@
   if ( PerDomainRSpam == 1 ) {
 #endif /* ENABLE_PER_DOMAIN */
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: prepare for rspamc: %s\n", message_name);
+      fprintf(stderr, "simscan:[%d]: prepare for rspamc: %s\n",getppid(), message_name);
     }
     if ( (fd = open(message_name, O_RDONLY)) == -1 ) {
       if ( DebugFlag > 0 ) {
-	fprintf(stderr, "simscan: can not open rspam file: %s\n", message_name);
+	fprintf(stderr, "simscan:[%d]: can not open rspam file: %s\n",getppid(), message_name);
       }
       exit_clean(EXIT_400);
     }
@@ -704,7 +704,7 @@
     /* set the standard input to be the new file */
     if ( fd_move(0,fd)  == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: could not fd_move rspam stdin\n");
+        fprintf(stderr, "simscan:[%d]: could not fd_move rspam stdin\n",getppid());
       }
       exit_clean(EXIT_400);
     }
@@ -712,7 +712,7 @@
     /* check for spam with RSPAM */
     snprintf(spam_message_name, sizeof(spam_message_name), "rspam.msg.%s", unique_ext);
     if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: check_rspam will now read %s and write to %s\n", message_name, spam_message_name);
+          fprintf(stderr, "simscan:[%d]: check_rspam will now read %s and write to %s\n",getppid(), message_name, spam_message_name);
     }
     ret = check_rspam();
     switch ( ret ) {
@@ -724,7 +724,7 @@
       /* spam detected, refuse message */
       case 1:
         if ( DebugFlag > 0 ) {
-	  fprintf(stderr, "simscan: RSPAM reported message as being SPAM\n");
+	  fprintf(stderr, "simscan:[%d]: RSPAM reported message as being SPAM\n",getppid());
 	}
 	close(fd);
 #ifdef QUARANTINEDIR
@@ -733,7 +733,7 @@
 
 #ifdef ENABLE_DROPMSG
 	if ( DebugFlag > 0 ) {
-	  fprintf(stderr, "simscan: dropping the message\n");
+	  fprintf(stderr, "simscan:[%d]: dropping the message\n",getppid());
 	}
 	exit_clean(EXIT_0);
 	/* Drop the message, returning success to sender. */
@@ -751,7 +751,7 @@
       /* rspam processed message and no spam detected */
       case 0:
 	if ( DebugFlag > 0 ) {
-	  fprintf(stderr, "simscan: RSPAM reported message %s as being HAM\n", spam_message_name);
+	  fprintf(stderr, "simscan:[%d]: RSPAM reported message %s as being HAM\n",getppid(), spam_message_name);
 	}
 	/* open the spam file read only */
 	strncpy(message_name,spam_message_name,BUFFER_SIZE);
@@ -760,7 +760,7 @@
 	/* errors , return temporary error */
       default:
         if ( DebugFlag > 0 ) {
-	  fprintf(stderr, "simscan: check_rspam had an error ret: %d\n", ret);
+	  fprintf(stderr, "simscan:[%d]: check_rspam had an error ret: %d\n",getppid(), ret);
 	}
 	close(fd);
 	exit_clean(EXIT_400);
@@ -768,7 +768,7 @@
 #ifdef ENABLE_PER_DOMAIN
   } 
   else {
-    fprintf(stderr, "simscan: skipping check_rspam, disabled for this domain\n");
+    fprintf(stderr, "simscan:[%d]: skipping check_rspam, disabled for this domain\n",getppid());
   }
 #endif /* ENABLE_PER_DOMAIN */
 #endif /* ENABLE_RSPAM */
@@ -778,13 +778,13 @@
 #ifdef ENABLE_PER_DOMAIN
   if ( PerDomainDSpam == 1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: prepare for dspamc: %s\n", message_name);
+      fprintf(stderr, "simscan:[%d]: prepare for dspamc: %s\n",getppid(), message_name);
     }
 #endif /* ENABLE_PER_DOMAIN */
     /* re-open the file read only */
     if ( (fd = open(message_name, O_RDONLY)) == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: dspam can not open file: %s\n", message_name);
+        fprintf(stderr, "simscan:[%d]: dspam can not open file: %s\n",getppid(), message_name);
       }
       exit_clean(EXIT_400);
     }
@@ -792,7 +792,7 @@
     /* set the standard input to be the new file */
     if ( fd_move(0,fd)  == -1 ) {
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: dspam could not fd_move\n");
+        fprintf(stderr, "simscan:[%d]: dspam could not fd_move\n",getppid());
       }
       exit_clean(EXIT_400);
     }
@@ -800,7 +800,7 @@
     /* optionally check for spam with DSPAM */
     snprintf(spam_message_name, sizeof(spam_message_name), "dspam.msg.%s", unique_ext);
     if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: check_dspam will now read %s and write to %s\n", message_name, spam_message_name);
+          fprintf(stderr, "simscan:[%d]: check_dspam will now read %s and write to %s\n",getppid(), message_name, spam_message_name);
     }
     ret = check_dspam();
     switch ( ret ) {
@@ -813,7 +813,7 @@
       /* spam detected, refuse message */
       case 1:
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: DSPAM reported message as being SPAM\n");
+          fprintf(stderr, "simscan:[%d]: DSPAM reported message as being SPAM\n",getppid());
         }
         close(fd);
 
@@ -824,7 +824,7 @@
 
 #ifdef ENABLE_DROPMSG
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: droping the message\n");
+          fprintf(stderr, "simscan:[%d]: droping the message\n",getppid());
         }
         exit_clean(EXIT_0);
         /* Drop the message, returning success to sender. */
@@ -842,7 +842,7 @@
         /* dspam processed message and no spam detected */
       case 0:
           if ( DebugFlag > 0 ) {
-                  fprintf(stderr, "simscan: DSPAM reported message as NOT being SPAM\n");
+                  fprintf(stderr, "simscan:[%d]: DSPAM reported message as NOT being SPAM\n",getppid());
           }
         /* open the spam file read only */
         strncpy(message_name,spam_message_name,BUFFER_SIZE);
@@ -850,7 +850,7 @@
         /* errors , return temporary error */
       default:
         if ( DebugFlag > 0 ) {
-          fprintf(stderr, "simscan: check_dspam had an error ret: %d\n", ret);
+          fprintf(stderr, "simscan:[%d]: check_dspam had an error ret: %d\n",getppid(), ret);
         }
         close(fd);
         exit_clean(EXIT_400);
@@ -858,7 +858,7 @@
 #ifdef ENABLE_PER_DOMAIN
   }
   else {
-    fprintf(stderr, "simscan: skipping check_dspam, disabled for this domain\n");
+    fprintf(stderr, "simscan:[%d]: skipping check_dspam, disabled for this domain\n",getppid());
   }
 #endif /* ENABLE_PER_DOMAIN */
 #endif /* ENABLE_DSPAM */
@@ -867,7 +867,7 @@
   /* re-open the file read only */
   if ( (fd = open(message_name, O_RDONLY)) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: could not re-open file: %s\n", message_name);
+      fprintf(stderr, "simscan:[%d]: could not re-open file: %s\n",getppid(), message_name);
     }
     exit_clean(EXIT_400);
   }
@@ -875,7 +875,7 @@
   /* re-open the address read only */
   if ( (fd_per = open(addr_name, O_RDONLY)) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: could not re-open address: %s\n", addr_name);
+      fprintf(stderr, "simscan:[%d]: could not re-open address: %s\n",getppid(), addr_name);
     }
     exit_clean(EXIT_400);
   }
@@ -883,12 +883,12 @@
   /* set the standard input to be the new file */
   if ( fd_move(1,fd_per)  == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: could not fd_move\n");
+      fprintf(stderr, "simscan:[%d]: could not fd_move\n",getppid());
     }
     exit_clean(EXIT_400);
   }
 
-  if ( DebugFlag > 0 ) fprintf(stderr, "simscan: done, execing qmail-queue\n");
+  if ( DebugFlag > 0 ) fprintf(stderr, "simscan:[%d]: done, execing qmail-queue\n",getppid());
 
   if ( pipe(pim) != 0 ) return(-1);
 
@@ -896,7 +896,7 @@
   switch(pid = vfork()) {
     case -1:
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: error forking qmail-queue\n");
+        fprintf(stderr, "simscan:[%d]: error forking qmail-queue\n",getppid());
       }
       close(pim[0]);
       close(pim[1]);
@@ -918,7 +918,7 @@
     strlen(runned_scanners) > 0 ? runned_scanners : "none");
   if ( write(pim[1], buffer,strlen(buffer)) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error writing received line\n");
+      fprintf(stderr, "simscan:[%d]: error writing received line\n",getppid());
     }
     exit_clean(EXIT_400);
   }
@@ -929,7 +929,7 @@
     if ( write(pim[1], buffer,ret) == -1 ) {
       if ( DebugFlag > 0 ) {
         fprintf(stderr, 
-          "simscan: error writing msg to qmail-queue error: %d\n", errno);
+          "simscan:[%d]: error writing msg to qmail-queue error: %d\n",getppid(), errno);
       }
       exit_clean(EXIT_400);
     }
@@ -940,14 +940,14 @@
   /* wait for qmail-queue to finish */
   if (waitpid(pid,&qstat, 0) == -1) { 
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error forking qmail-queue (back in simscan)\n");
+      fprintf(stderr, "simscan:[%d]: error forking qmail-queue (back in simscan)\n",getppid());
     }
     exit_clean(EXIT_400);
   }
 
   /* hand the email to the qmail-queue */
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: qmail-queue exited %d\n", WEXITSTATUS(qstat));
+    fprintf(stderr, "simscan:[%d]: qmail-queue exited %d\n",getppid(), WEXITSTATUS(qstat));
   }
 
   /* remove the working files */
@@ -1005,7 +1005,7 @@
   char tmpbuf[256];
   char *data;
   
-  if ( DebugFlag > 1 ) fprintf(stderr, "simscan: cdb looking up version %s\n", key);  
+  if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: cdb looking up version %s\n",getppid(), key);  
 
   snprintf(tmpbuf,sizeof(tmpbuf), "%s/simversions.cdb", CONTROLDIR);
   if ( (fd = open(tmpbuf, O_RDONLY)) == -1 ) {
@@ -1015,7 +1015,7 @@
   ret = cdb_seek(fd,key,keylen,&dlen);
   if ( ret <= 0 ) {
     close(fd);
-    if (DebugFlag > 0) fprintf(stderr,"simscan: found no key: %s\n",key);
+    if (DebugFlag > 0) fprintf(stderr,"simscan:[%d]: found no key: %s\n",getppid(),key);
     return;
   }
   data = calloc(sizeof(char),dlen+1);
@@ -1024,8 +1024,8 @@
   ret = read(fd,data,dlen);
   close(fd);
   snprintf(runned_scanners+strlen(runned_scanners),MAX_EMAIL-strlen(runned_scanners)," %s: %s",key,data);
-  if ( DebugFlag > 2 ) fprintf(stderr, "simscan: runned_scanners is %s\n", runned_scanners);  
-  if ( DebugFlag > 2 ) fprintf(stderr, "simscan: found %s\n", data);  
+  if ( DebugFlag > 2 ) fprintf(stderr, "simscan:[%d]: runned_scanners is %s\n",getppid(), runned_scanners);  
+  if ( DebugFlag > 2 ) fprintf(stderr, "simscan:[%d]: found %s\n",getppid(), data);  
 }
 #endif
 
@@ -1085,7 +1085,7 @@
 #endif
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling clamdscan\n");
+    fprintf(stderr, "simscan:[%d]: calling clamdscan\n",getppid());
   }
 
   if ( pipe(pim) != 0 ) return(-1);
@@ -1149,7 +1149,7 @@
 		{
 			if ( needh )
 			{
-				fputs ( "simscan: clamdscan: " , stderr ) ;
+				fprintf (stderr,"simscan:[%d]: clamdscan ",getppid()) ;
 				needh = 0 ;
 			}
 
@@ -1236,14 +1236,14 @@
 
   if ( (rspam_fd=open(spam_message_name, O_RDWR|O_CREAT|O_TRUNC,0644)) ==- 1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: check_rspam could not open file: %s\n",
-        spam_message_name);
+      fprintf(stderr, "simscan:[%d]: check_rspam could not open file: %s\n",
+        getppid(),spam_message_name);
     }
     return(-1);
   }
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling rspamc\n");
+    fprintf(stderr, "simscan:[%d]: calling rspamc\n",getppid());
   }
 
   // RSPAMC command
@@ -1260,7 +1260,7 @@
   rspamc_args[i] = NULL;
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling %s ", RSPAMC);
+    fprintf(stderr, "simscan:[%d]: calling %s ",getppid(), RSPAMC);
     i=0;
     while(rspamc_args[i] != NULL){
       fprintf(stderr, "%s, ",rspamc_args[i]);
@@ -1312,7 +1312,7 @@
     return(-1);
   }
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, " simscan: rspamc done\n");
+    fprintf(stderr, " simscan:[%d]: rspamc done\n",getppid());
   }
 
   /* check if the child died on a signal */
@@ -1355,7 +1355,7 @@
     if ( PerDomainSpamPassthru == 1) {
       if (( IsSpam == 1 ) && (DebugFlag > 0)){
         fprintf(stderr,
-          "simscan: delivering spam because spam-passthru is defined in this domain\n");
+          "simscan:[%d]: delivering spam because spam-passthru is defined in this domain\n",getppid());
       }
       log_message("PASSTHRU", Subject,1);
       return(0);
@@ -1432,14 +1432,14 @@
 
   if ( (spam_fd=open(spam_message_name, O_RDWR|O_CREAT|O_TRUNC,0644)) ==- 1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: check_dspam could not open spam file: %s\n",
-        spam_message_name);
+      fprintf(stderr, "simscan:[%d]: check_dspam could not open spam file: %s\n",
+        getppid(),spam_message_name);
     }
     return(-1);
   }
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling dspamc\n");
+    fprintf(stderr, "simscan:[%d]: calling dspamc\n",getppid());
   }
 
   tmpbuf = malloc(strlen(DSPAM_ARGS)+1);
@@ -1479,7 +1479,7 @@
   dspam_args[i++] = NULL;
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling %s ", DSPAM);
+    fprintf(stderr, "simscan:[%d]: calling %s ",getppid(), DSPAM);
     i=0;
     while(dspam_args[i] != NULL){
       fprintf(stderr, " %s", dspam_args[i]);
@@ -1494,14 +1494,14 @@
         close(pim[0]);
         close(pim[1]);
 	close(spam_fd);
-        fprintf(stderr,"simscan: failed to fork dspam\n");
+        fprintf(stderr,"simscan:[%d]: failed to fork dspam\n",getppid());
         return(-1);
       case 0:
         close(pim[0]);
         dup2(pim[1],1);
         close(pim[1]);
         i = execve(DSPAM, dspam_args, 0);
-	fprintf(stderr, "simscan: execve returned %d/%d\n", i, errno);
+	fprintf(stderr, "simscan:[%d]: execve returned %d/%d\n",getppid(), i, errno);
         _exit(-1);
     }
     close(pim[1]);
@@ -1521,13 +1521,13 @@
   memset(buffer,0,sizeof(buffer));
   spamfs = fdopen(0,"r");
   if (spamfs == NULL ) {
-    if ( DebugFlag > 0 ) fprintf(stderr, "simscan: dspam (check_dspam) error opening stdin, error=%d\n",errno);
+    if ( DebugFlag > 0 ) fprintf(stderr, "simscan:[%d]: dspam (check_dspam) error opening stdin, error=%d\n",getppid(),errno);
   }
   got_data = 0;
   while(1) {
     fret = fgets(buffer,sizeof(buffer)-1,spamfs);
     if (fret == NULL ) {
-       if ( DebugFlag > 0 && readno == 0 ) fprintf(stderr, "simscan: dspam (check_dspam) reading header line: %d, check dspam.conf perms\n",readno);
+       if ( DebugFlag > 0 && readno == 0 ) fprintf(stderr, "simscan:[%d]: dspam (check_dspam) reading header line: %d, check dspam.conf perms\n",getppid(),readno);
        break;
     } 
     readno++;
@@ -1548,7 +1548,7 @@
   /* wait for dspam to finish */
   if (waitpid(pid,&rmstat, 0) == -1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: Error waiting for dspamc\n");
+      fprintf(stderr, "simscan:[%d]: Error waiting for dspamc\n",getppid());
     }
     return(-1);
   }
@@ -1556,13 +1556,13 @@
   /* check if the child died on a signal */
   if ( WIFSIGNALED(rmstat) ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: dspamc child died on a signal\n");
+      fprintf(stderr, "simscan:[%d]: dspamc child died on a signal\n",getppid());
     }    
     return(-1);
   }
   if ( got_data == 0 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: dspamc got no data from dspam\n");
+      fprintf(stderr, "simscan:[%d]: dspamc got no data from dspam\n",getppid());
     }
     return(-1);
   }
@@ -1577,7 +1577,7 @@
       if ( IsSpam == 1 ) {
         if (DebugFlag > 0) {
           fprintf(stderr,
-            "simscan: delivering spam because spam-passthru is defined in this domain\n");
+            "simscan:[%d]: delivering spam because spam-passthru is defined in this domain\n",getppid());
         }
         log_message("PASSTHRU", Subject,1);
       } else {
@@ -1657,14 +1657,14 @@
   
   if ( (spam_fd=open(spam_message_name, O_RDWR|O_CREAT|O_TRUNC,0644)) ==- 1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: check_spam could not open spam file: %s\n", 
-        spam_message_name);
+      fprintf(stderr, "simscan:[%d]: check_spam could not open spam file: %s\n", 
+        getppid(),spam_message_name);
     }
     return(-1);
   }
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling spamc\n");
+    fprintf(stderr, "simscan:[%d]: calling spamc\n",getppid());
   }
 
   tmpbuf = malloc(strlen(SPAMC_ARGS)+1);
@@ -1690,7 +1690,7 @@
   spamc_args[i] = NULL;
 
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: calling %s ", SPAMC);
+    fprintf(stderr, "simscan:[%d]: calling %s ",getppid(), SPAMC);
     i=0;
     while(spamc_args[i] != NULL){
       fprintf(stderr, " %s", spamc_args[i]);
@@ -1782,7 +1782,7 @@
     if ( PerDomainSpamPassthru == 1) {
       if (( IsSpam == 1 ) && (DebugFlag > 0)){	    
         fprintf(stderr, 
-          "simscan: delivering spam because spam-passthru is defined in this domain\n");
+          "simscan:[%d]: delivering spam because spam-passthru is defined in this domain\n",getppid());
       }	
       log_message("PASSTHRU", Subject,1);
       return(0);
@@ -1945,14 +1945,14 @@
   strncat(quarantinefile, "/", sizeof(quarantinefile)-1);
   strncat(quarantinefile, message_name, sizeof(quarantinefile)-1);
   
-  fprintf(stderr, "simscan: Putting the message in quarantine: %s\n", 
-    quarantinefile);
+  fprintf(stderr, "simscan:[%d]: Putting the message in quarantine: %s\n", 
+    getppid(),quarantinefile);
   
   if ((fd_destino=open(quarantinefile, O_WRONLY|O_CREAT|O_TRUNC, 0644)) == -1) {
     if ( DebugFlag > 0 ) {
       fprintf(stderr, 
-        "simscan: error opening quarantine file %s errnostr: %d\n", 
-        message_name, errno);
+        "simscan:[%d]: error opening quarantine file %s errnostr: %d\n", 
+        getppid(),message_name, errno);
     }
     return;
   }
@@ -1961,7 +1961,7 @@
   while ((ret = read(fd_origem, buffer, sizeof(buffer))) > 0) {
     if (write(fd_destino, buffer, ret) == -1) {
       if (DebugFlag > 0) {
-        fprintf(stderr, "simscan: error writing msg error: %d\n", errno);
+        fprintf(stderr, "simscan:[%d]: error writing msg error: %d\n",getppid(), errno);
       }
       close(fd_origem);
       close(fd_destino);
@@ -1971,20 +1971,20 @@
   
   if (close(fd_origem) == -1) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error closing original mail file errno: %d\n", 
-        errno);
+      fprintf(stderr, "simscan:[%d]: error closing original mail file errno: %d\n", 
+        getppid(),errno);
     }
     return;
   }
   
   if ( close(fd_destino) == -1 ) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: error closing quarantine file errno: %d\n", 
-        errno);
+      fprintf(stderr, "simscan:[%d]: error closing quarantine file errno: %d\n", 
+        getppid(),errno);
     }
     return;
   } else {
-    fprintf(stderr, "simscan: Message recorded in quarantine successful\n");
+    fprintf(stderr, "simscan:[%d]: Message recorded in quarantine successful\n",getppid());
   }
   return;
 }
@@ -1999,7 +1999,7 @@
   if ( DebugFiles < 1 )
     remove_files(workdir);
   if ( DebugFlag > 0 ) {
-    fprintf(stderr, "simscan: exit error code: %d\n", error_code); 
+    fprintf(stderr, "simscan:[%d]: exit error code: %d\n",getppid(), error_code); 
   }
   _exit(error_code);
 }
@@ -2046,12 +2046,12 @@
  char *found;
   
   MaxAttach = 0;
-  if ( DebugFlag > 3 ) fprintf(stderr, "simscan: add_attach called with %s\n", list);  
+  if ( DebugFlag > 3 ) fprintf(stderr, "simscan:[%d]: add_attach called with %s\n",getppid(), list);  
   while( ( found = strsep(&list,":") ) != NULL) {
     strncpy(bk_attachments[MaxAttach], found, strlen(found));
     if ( DebugFlag > 1 ) {
-      fprintf(stderr, "simscan: %s is attachment number %d\n", 
-      bk_attachments[MaxAttach], MaxAttach);  
+      fprintf(stderr, "simscan:[%d]: %s is attachment number %d\n", 
+      getppid(),bk_attachments[MaxAttach], MaxAttach);  
     }
     ++MaxAttach;
   }
@@ -2081,7 +2081,7 @@
     }
 
     for(i=0;i<MaxAttach;++i) {
-      if ( DebugFlag > 2 ) fprintf(stderr, "simscan: checking attachment %s against %s\n", mydirent->d_name, bk_attachments[i] );  
+      if ( DebugFlag > 2 ) fprintf(stderr, "simscan:[%d]: checking attachment %s against %s\n",getppid(), mydirent->d_name, bk_attachments[i] );  
       lowerit(mydirent->d_name); 
       if ( str_rstr(mydirent->d_name,bk_attachments[i]) == 0 ) {
         strncpy(AttachName, mydirent->d_name, sizeof(AttachName)-1); 
@@ -2140,7 +2140,7 @@
  char *parm;
  char *val;
   
-  if ( DebugFlag > 1 ) fprintf(stderr, "simscan: cdb looking up %s\n", key);  
+  if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: cdb looking up %s\n",getppid(), key);  
 
   snprintf(tmpbuf,sizeof(tmpbuf), "%s/simcontrol.cdb", CONTROLDIR);
   if ( (fd = open(tmpbuf, O_RDONLY)) == -1 ) {
@@ -2158,19 +2158,19 @@
   ret = read(fd,data,dlen);
   close(fd);
   
-  if ( DebugFlag > 1 ) fprintf(stderr, "simscan: cdb for %s found %s\n", key, data); 
+  if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: cdb for %s found %s\n",getppid(), key, data); 
 
   parm = strsep(&data, PER_DOMAIN_TOKENS);
   if ( parm != NULL ) val = strsep(&data, PER_DOMAIN_TOKENS);
   while ( parm != NULL && val != NULL) {
-    if ( DebugFlag > 1 ) fprintf(stderr, "simscan: pelookup %s = %s\n", parm,val);  
+    if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: pelookup %s = %s\n",getppid(), parm,val);  
     if ( strcasecmp(parm,"rspam") == 0 ) {
       if ( strcasecmp(val, "yes") == 0 ) {
         PerDomainRSpam = 1; 
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainRSpam = 0; 
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: rspam = %s\n", (PerDomainRSpam ? "enabled" : "disabled"));
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: rspam = %s\n",getppid(), (PerDomainRSpam ? "enabled" : "disabled"));
     }
     else if ( strcasecmp(parm,"dspam") == 0 ) {
       if ( strcasecmp(val, "yes") == 0 ) {
@@ -2178,11 +2178,11 @@
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainDSpam = 0;
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: dspam = %s\n", (PerDomainDSpam ? "enabled" : "disabled"));
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: dspam = %s\n",getppid(), (PerDomainDSpam ? "enabled" : "disabled"));
     }
     else if ( strcasecmp(parm,"dspamuser") == 0 ) {
       strncpy(dspamuser,val,BUFFER_SIZE);
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: dspamuser = %s\n", dspamuser);
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: dspamuser = %s\n",getppid(), dspamuser);
     }
     else if ( strcasecmp(parm,"clam") == 0 ) {
       if ( strcasecmp(val, "yes") == 0 ) {
@@ -2190,7 +2190,7 @@
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainClam = 0; 
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: clam = %s\n", (PerDomainClam ? "enabled" : "disabled"));
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: clam = %s\n",getppid(), (PerDomainClam ? "enabled" : "disabled"));
     } 
     else if ( strcasecmp(parm,"spam") == 0 ) {
       if ( strcasecmp(val, "yes") == 0 ) {
@@ -2198,17 +2198,17 @@
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainSpam = 0; 
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: spam = %s\n", (PerDomainSpam ? "enabled" : "disabled"));
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: spam = %s\n",getppid(), (PerDomainSpam ? "enabled" : "disabled"));
     } 
     else if ( strcasecmp(parm,"qmailqueue") == 0 ) {
       qmail_queue = strdup(val);
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: qmailqueue = %s\n", val);
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: qmailqueue = %s\n",getppid(), val);
 
 #ifdef ENABLE_SPAM
     } 
     else if ( strcasecmp(parm,"spamuser") == 0 ) {
       strncpy(spamuser,val,BUFFER_SIZE);
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: spamuser = %s\n", spamuser);
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: spamuser = %s\n",getppid(), spamuser);
 #endif
 
     } 
@@ -2218,17 +2218,17 @@
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainTrophie = 0; 
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: trophie = %s/%d\n", val,PerDomainTrophie);  
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: trophie = %s/%d\n",getppid(), val,PerDomainTrophie);  
 #ifdef ENABLE_REGEX
     } 
     else if ( strcasecmp(parm,"regex") == 0 ) {
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: regex flag %s = %s\n", parm, val);  
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: regex flag %s = %s\n",getppid(), parm, val);  
       init_regex(val);
 #endif
 #ifdef ENABLE_ATTACH
     } 
     else if ( strcasecmp(parm,"attach") == 0 ) {
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: attachment flag %s = %s\n", parm, val);  
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: attachment flag %s = %s\n",getppid(), parm, val);  
       add_attach(val);
 #endif
 #ifdef SPAM_HITS
@@ -2236,7 +2236,7 @@
     else if ( strcasecmp(parm,"spam_hits") == 0 ) {
       PerDomainHits = 1;
       PDHits = atof(val);
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: Per Domain Hits set to : %f\n", PDHits);
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: Per Domain Hits set to : %f\n",getppid(), PDHits);
 #endif
 #ifdef ENABLE_SPAM_PASSTHRU
     } 
@@ -2246,11 +2246,11 @@
       } else if ( strcasecmp(val, "no") == 0 ) {
         PerDomainSpamPassthru = 0;
       }
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: spampassthru = %s/%d\n", val,PerDomainSpamPassthru);
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: spampassthru = %s/%d\n",getppid(), val,PerDomainSpamPassthru);
 #endif   
     } 
     else {
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: unimplemented flag %s = %s\n", parm, val);  
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: unimplemented flag %s = %s\n",getppid(), parm, val);  
     }
     parm = strsep(&data, PER_DOMAIN_TOKENS);
     if ( parm != NULL ) val = strsep(&data, PER_DOMAIN_TOKENS);
@@ -2283,25 +2283,25 @@
   int i,keyIndex=0;
 
   *localtmp='\0';
-  if ( DebugFlag > 1 ) fprintf(stderr, "simscan: pelookup: called with %s\n", email);  
+  if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: pelookup: called with %s\n",getppid(), email);  
 
   /* first we lookup the domain */
   
   for(tmpstr = email; tmpstr!=NULL && *tmpstr!='@' && keyIndex < MAX_EMAIL; ++tmpstr ){++keyIndex;}
   if (*tmpstr != '@') {
-    if ( DebugFlag > 1 ) fprintf(stderr, "simscan: WARN: no domain part found! %s\n", email);  
+    if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: WARN: no domain part found! %s\n",getppid(), email);  
     *domain='\0';
   } else {
     keyIndex++;
     strncpy(domain,email+keyIndex,sizeof(domain)-keyIndex-1); 
-    if ( DebugFlag > 1 ) fprintf(stderr, "simscan: pelookup: domain is %s\n", domain);  
+    if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: pelookup: domain is %s\n",getppid(), domain);  
     per_domain_lookup( domain ); 
   }
  
   strncpy(local,email, sizeof(local)); 
   for(l_ptr=local; l_ptr!=NULL && *l_ptr!='@' && *l_ptr!='\0'; ++l_ptr );
   *l_ptr='\0';
-  if ( DebugFlag > 1 ) fprintf(stderr, "simscan: pelookup: local part is %s\n", local);  
+  if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: pelookup: local part is %s\n",getppid(), local);  
 
   /* then we check if the local part is an extended address (bla-ext)
    * we have to fill an array, as per_domain_lookup uses strtok again
@@ -2309,7 +2309,7 @@
   l_ptr = local;
   keyIndex=0;
   while( (lpart = strsep(&l_ptr,MAIL_EXT_TOKENS) )!= NULL && keyIndex < MAX_RCPT_TO) {
-    if ( DebugFlag > 2 ) fprintf(stderr, "simscan: lpart: local part is *%s*\n", localtmp);
+    if ( DebugFlag > 2 ) fprintf(stderr, "simscan:[%d]: lpart: local part is *%s*\n",getppid(), localtmp);
     strncat(localtmp,lpart,MAX_EMAIL-strlen(localtmp)-strlen(lpart)-1);
     sprintf(toScan[keyIndex], "%s@%s", localtmp,domain);
     strncat(localtmp,"-",MAX_EMAIL-strlen(localtmp)-2);
@@ -2345,23 +2345,23 @@
 
   if ( spambuf[0] == '\n' || spambuf[1] == '\n' ) {
     InHeaders = 0;
-    if ( DebugFlag > 0 ) fprintf(stderr,"simscan: dspam (is_dspam) in email header\n");
+    if ( DebugFlag > 0 ) fprintf(stderr,"simscan:[%d]: dspam (is_dspam) in email header\n",getppid());
     return(0);
   }
 
   if (strstr(spambuf, "X-DSPAM-Result:")) {
     if ( strstr(spambuf, "X-DSPAM-Result: Spam")) {
-      if ( DebugFlag > 0 ) fprintf(stderr,"simscan: dspam (is_dspam) IsSpam=1\n");
+      if ( DebugFlag > 0 ) fprintf(stderr,"simscan:[%d]: dspam (is_dspam) IsSpam=1\n",getppid());
       IsSpam=1;
     }
   } else if ( strncmp(spambuf, "X-DSPAM-Probability:", 19 ) == 0 ) {
     DSpamProbability = atof(&spambuf[20]);
-    fprintf(stderr,"simscan: dspam (is_dspam) Probability:%f\n",DSpamProbability);
+    fprintf(stderr,"simscan:[%d]: dspam (is_dspam) Probability:%f\n",getppid(),DSpamProbability);
   } else if ( strncmp(spambuf, "X-DSPAM-Confidence:", 19 ) == 0 ) {
     DSpamConf = atof(&spambuf[19]);
-    fprintf(stderr,"simscan: dspam (is_dspam) Confidence:%f\n",DSpamConf);
+    fprintf(stderr,"simscan:[%d]: dspam (is_dspam) Confidence:%f\n",getppid(),DSpamConf);
   } else if ( strncmp(spambuf, "Subject:", 8 ) == 0 ) {
-    fprintf(stderr,"simscan: dspam (is_dspam) Subject:%s\n",Subject);
+    fprintf(stderr,"simscan:[%d]: dspam (is_dspam) Subject:%s\n",getppid(),Subject);
     strncpy(Subject, &spambuf[9], sizeof(Subject)-1);
 
     /* replace : char with _ and null terminate on
@@ -2432,7 +2432,7 @@
 		  hits[l] = *tmpstr;
 	  }
 	  ReqHits = atof(hits);
-	  if (DebugFlag > 0) fprintf(stderr, "simscan: rspam: SpamHits: %f / %f required\n", SpamHits, ReqHits);
+	  if (DebugFlag > 0) fprintf(stderr, "simscan:[%d]: rspam: SpamHits: %f / %f required\n", getppid(),SpamHits, ReqHits);
   }
   return(0);
 }
@@ -2489,7 +2489,7 @@
        if ( tmpstr == NULL ) {
          if ( DebugFlag > 1 ) {
            fprintf(stderr,
-           "simscan: neither hits= or score= in X-Spam-Status header\n");
+           "simscan:[%d]: neither hits= or score= in X-Spam-Status header\n",getppid());
          }
          return(0);
        }
@@ -2500,7 +2500,7 @@
     if ( tmpstr == NULL ) {
        if ( DebugFlag > 1 ) {
          fprintf(stderr,
-           "simscan: neither hits= or score= in X-Spam-Status header\n");
+           "simscan:[%d]: neither hits= or score= in X-Spam-Status header\n",getppid());
        }
     }
     memset(hits,0,sizeof(hits));
@@ -2541,19 +2541,19 @@
   int match=0;
 
   for (i=0;i<numRegex;i++){
-      if ( DebugFlag > 1 ) { fprintf(stderr, "simscan: compiling regex %d (%s)\n", i,regexs[i]);  }
+      if ( DebugFlag > 1 ) { fprintf(stderr, "simscan:[%d]: compiling regex %d (%s)\n",getppid(), i,regexs[i]);  }
       comp_regexs[i]=pcre_compile(regexs[i], 0, &error, &erroffset, NULL);    
       if (comp_regexs[i] == NULL){
-        if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: error compiling regex %d (%s): %s\n", i,regexs[i],error);  }
+        if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: error compiling regex %d (%s): %s\n",getppid(), i,regexs[i],error);  }
       }
   }
   
-  if ( DebugFlag > 1 ) { fprintf(stderr, "simscan: regex opening message file %s\n", message_name);  }
+  if ( DebugFlag > 1 ) { fprintf(stderr, "simscan:[%d]: regex opening message file %s\n",getppid(), message_name);  }
   if ( (regex_fd=fopen(message_name, "r")) == NULL ) {
-    if ( DebugFlag > 1 ) { fprintf(stderr, "simscan: regex error opening message file %s\n", message_name);  }
+    if ( DebugFlag > 1 ) { fprintf(stderr, "simscan:[%d]: regex error opening message file %s\n",getppid(), message_name);  }
     retvalue=1;
   } else {
-    if ( DebugFlag > 1 ) { fprintf(stderr, "simscan: regex reading message\n");  }
+    if ( DebugFlag > 1 ) { fprintf(stderr, "simscan:[%d]: regex reading message\n",getppid());  }
     while(!feof(regex_fd) && !match){
       /* read line and match it */
       fgets(line, MAX_REGEX_LINE, regex_fd);
@@ -2562,13 +2562,13 @@
           if (comp_regexs[i] != NULL){
             rc=pcre_exec(comp_regexs[i], NULL, line, strlen(line), 0, 0, NULL,0);
             if (rc >= 0){
-              if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: regex match %d (%s) matches %s\n", i, regexs[i],line);  };
+              if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: regex match %d (%s) matches %s\n",getppid(), i, regexs[i],line);  };
               match=1;
               retvalue=2;
               snprintf(VirusName,BUFFER_SIZE,"#%d",i);
             } else if (rc < -1){
               /* -1 means no match, but all other errors are ``strange'' */
-              if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: regex %d (%s) error %d\n", i, regexs[i],rc);  };
+              if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: regex %d (%s) error %d\n",getppid(), i, regexs[i],rc);  };
             }
           }
         }
@@ -2576,7 +2576,7 @@
     }
   }
   
-  if ( DebugFlag > 0 ) { fprintf(stderr, "simscan: regex freeing memory\n");  };
+  if ( DebugFlag > 0 ) { fprintf(stderr, "simscan:[%d]: regex freeing memory\n",getppid());  };
   for (i=0;i<numRegex;i++){
     pcre_free(comp_regexs[i]);
   }
@@ -2591,10 +2591,10 @@
 void init_regex (char *list) {
   int len=0;
   char *found;
-  if ( DebugFlag > 3 ) fprintf(stderr, "simscan: init_regex called with %s\n", list);  
+  if ( DebugFlag > 3 ) fprintf(stderr, "simscan:[%d]: init_regex called with %s\n",getppid(), list);  
   while( ( found = strsep(&list,":") ) != NULL) {
       strncpy(regexs[numRegex], found, strlen(found));
-      if ( DebugFlag > 1 ) fprintf(stderr, "simscan: regex %d is %s\n", numRegex,regexs[numRegex]);  
+      if ( DebugFlag > 1 ) fprintf(stderr, "simscan:[%d]: regex %d is %s\n",getppid(), numRegex,regexs[numRegex]);  
       ++numRegex;
   }
 }
@@ -2622,7 +2622,7 @@
 #endif
       
   if ( DebugFlag > 1 ) {
-    fprintf(stderr, "simscan: trophie starting!\n");  
+    fprintf(stderr, "simscan:[%d]: trophie starting!\n",getppid());  
   }
 
   /* Create socket */
@@ -2644,7 +2644,7 @@
   strncat(command, "\n", sizeof(command)-1);
 
   if ( DebugFlag > 1 ) {
-    fprintf(stderr, "simscan: sending command [%s]\n",command);  
+    fprintf(stderr, "simscan:[%d]: sending command [%s]\n",getppid(),command);  
   }
 
   if (retvalue == 0 && write(sock, command, strlen(command)) < 0 ) {
@@ -2659,31 +2659,31 @@
     if (buffer[0] == '1') {
       strncpy(VirusName,buffer+2,sizeof(VirusName)); 
       if ( DebugFlag > 0 ) {
-        fprintf(stderr, "simscan: trophie, virus found [%s]\n",VirusName);  
+        fprintf(stderr, "simscan:[%d]: trophie, virus found [%s]\n",getppid(),VirusName);  
       }
       retvalue=1;
     } else if (!strncmp(buffer, "-1", 2)) {
-      if ( DebugFlag > 0 ) fprintf(stderr, "simscan: trophie, error scanning file!\n"); 
+      if ( DebugFlag > 0 ) fprintf(stderr, "simscan:[%d]: trophie, error scanning file!\n",getppid()); 
       retvalue=5;
     } else if (!strncmp(buffer, "-2", 2)) {
-      if ( DebugFlag > 0 ) fprintf(stderr, "simscan: trophie, error scanning file!\n"); 
+      if ( DebugFlag > 0 ) fprintf(stderr, "simscan:[%d]: trophie, error scanning file!\n",getppid()); 
       retvalue=5;
     } else {
       if ( DebugFlag > 1 ) {
-        fprintf(stderr, "simscan: trophie, file clean\n");  
+        fprintf(stderr, "simscan:[%d]: trophie, file clean\n",getppid());  
       }
       retvalue=0;
     }
   } else if (retvalue == 0) {
     if ( DebugFlag > 0 ) {
-      fprintf(stderr, "simscan: trophie, error reading from socket\n");  
+      fprintf(stderr, "simscan:[%d]: trophie, error reading from socket\n",getppid());  
     }
     retvalue=4;
   }
   
   close(sock);
   if ( DebugFlag > 1 ) {
-    fprintf(stderr, "simscan: trophie ending, retvalue = %d\n", retvalue);  
+    fprintf(stderr, "simscan:[%d]: trophie ending, retvalue = %d\n",getppid(), retvalue);  
   }
 
 #ifdef ENABLE_RECEIVED
